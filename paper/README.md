Paper build instructions

To compile the LaTeX draft to PDF (if you have a LaTeX toolchain installed):

```bash
cd paper
pdflatex valorant_analysis.tex
```

If `pdflatex` is unavailable, you can convert the PDF with an online LaTeX service or install `texlive`.

The draft references figures produced by the analysis scripts in `analysis/` and `analysis/advanced_outputs/figures`.
